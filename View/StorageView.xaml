<UserControl x:Class="ZooMania.View.StorageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ZooMania.View"
             xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Loaded="UserControl_Loaded">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="100"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        
        <!--dodanie towaru
        usuniecie towaru
        wyswietlenie towaru
        wybor kuriera
        podanie metody platnoci - pobranie
        plany dostaw
        karton,paleta-->

        <StackPanel Grid.Column="0" Grid.Row="0"  Grid.ColumnSpan="2" Margin="15">

            <TextBlock Text="Wybierz produkt"
                       Foreground="White"
                       FontSize="16"
                       FontWeight="Medium"
                       FontFamily="Montserrat"
                       Margin="0,0,0,15"/>

            <ComboBox Name="cbProdukty" Margin="0,0,20,5"/>

        </StackPanel>

        <StackPanel Grid.Column="2" Grid.Row="0" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="10">

            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <Button Content="-" Width="30" Click="DecreaseButton_Click"/>
                <TextBox Text="{Binding Quantity}" Width="100" Name="txtQuantity" IsReadOnly="True" TextAlignment="Center"/>
                <Button Content="+" Width="30" Click="IncreaseButton_Click"/>
            </StackPanel>

            <Button Content="Zmień stany" Click="DodajDoStorage" Margin="0,5,0,0" Style="{StaticResource GreenButton}"/>

        </StackPanel>

        <StackPanel Grid.Column="0" Grid.Row="1" Grid.ColumnSpan="2" Margin="15">

            <DataGrid x:Name="dgMagazyn" AutoGenerateColumns="False" Height="320">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" />
                    <DataGridTextColumn Header="Nazwa" Binding="{Binding Name}" />
                    <DataGridTextColumn Header="Cena" Binding="{Binding Price}" />
                    <DataGridTextColumn Header="Kategoria" Binding="{Binding CategoryName}" />
                    <DataGridTextColumn Header="Dostępność" Binding="{Binding Quantity}" />
                </DataGrid.Columns>
            </DataGrid>

        </StackPanel>

        <StackPanel Grid.Column="2" Grid.Row="1">
            
            <Button Margin="15" Click="RefreshButton_Click" Style="{StaticResource GreenButtonStyle}">
                <fa:IconImage Icon="Refresh" Height="20" Foreground="White"/>
            </Button>
            
        </StackPanel>

        


    </Grid>
</UserControl>
