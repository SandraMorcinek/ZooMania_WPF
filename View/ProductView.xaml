<UserControl x:Class="ZooMania.View.ProductView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ZooMania.View"
             xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp" 
             mc:Ignorable="d" 
             Loaded="Window_Loaded"
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" Margin="25">

            <TextBlock Text="Dodaj produkt"
                       Foreground="White"
                       FontSize="16"
                       FontWeight="Medium"
                       FontFamily="Montserrat"
                       Margin="0,0,0,20"/>

            <TextBlock Text="Nazwa produktu:"
                       Foreground="White"
                       FontSize="12"
                       FontWeight="Medium"
                       FontFamily="Montserrat"
                       Margin="0,0,0,5"/>
            <TextBox Name="txtNazwaProduktu" Margin="0,0,0,5"/>

            <TextBlock Text="Cena:"
                       Foreground="White"
                       FontSize="12"
                       FontWeight="Medium"
                       FontFamily="Montserrat"
                       Margin="0,0,0,5"/>
            <TextBox Name="txtCena" Margin="0,0,0,5"/>

            <TextBlock Text="Opis:"
                       Foreground="White"
                       FontSize="12"
                       FontWeight="Medium"
                       FontFamily="Montserrat"
                       Margin="0,0,0,5"/>
            <TextBox Name="txtOpis" Margin="0,0,0,5" Height="100" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"/>

            <TextBlock Text="Kategoria:"
                       Foreground="White"
                       FontSize="12"
                       FontWeight="Medium"
                       FontFamily="Montserrat"
                       Margin="0,0,0,5"/>
            <ComboBox Name="cbKategoria" Margin="0,0,0,5"></ComboBox>

            <Button x:Name="btnWybierzZdjecie" Content="Wybierz zdjęcie" Click="btnWybierzZdjecie_Click" Margin="0,10,0,5" Style="{StaticResource GreenButton}"/>

            <Image Name="imgProdukt" Height="100" Width="100" Margin="0,5,0,0"/>

            <Button Content="Dodaj produkt" Click="DodajProdukt_Click" Margin="15" Style="{StaticResource GreenButton}"/>

        </StackPanel>

        

        <StackPanel Grid.Column="1" Margin="25">

            <Button Margin="15" Click="RefreshButton_Click" Style="{StaticResource GreenButtonStyle}">
                <fa:IconImage Icon="Refresh" Height="20" Foreground="White"/>
            </Button>

            <DataGrid x:Name="dgProdukty" AutoGenerateColumns="False" Height="335">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Id" Binding="{Binding Id}" />
                    <DataGridTextColumn Header="Nazwa" Binding="{Binding Name}" />

                    <DataGridTemplateColumn Header="Image">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Image Width="50" Height="50" Source="{Binding ImageData}" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTextColumn Header="Cena" Binding="{Binding Price}" />
                    <DataGridTextColumn Header="Opis" Binding="{Binding Description}" />
                    <DataGridTextColumn Header="Kategoria" Binding="{Binding CategoryName}" />
                    <DataGridTextColumn Header="CategoryId" Binding="{Binding CategoryId}" Visibility="Collapsed" />
                </DataGrid.Columns>
            </DataGrid>

        </StackPanel>

        <StackPanel Grid.Column="2" Margin="25">

            <TextBlock Text="Edytuj produkt"
                       Foreground="White"
                       FontSize="16"
                       FontWeight="Medium"
                       FontFamily="Montserrat"
                       Margin="0,0,0,20"/>

            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                
                <TextBlock Text="Id:"
                           Foreground="White"
                           FontSize="12"
                           FontWeight="Medium"
                           FontFamily="Montserrat"
                           Margin="0,0,15,0"/>

                <TextBox x:Name="txtIdProduktu" Margin="0,0,15,0" Width="100"/>

                <Button Content="Wczytaj" Click="WczytajProdukt_Click" Style="{StaticResource GreenButton}" Width="60"/>
            </StackPanel>

            <TextBlock Text="Nazwa produktu:"
                       Foreground="White"
                       FontSize="12"
                       FontWeight="Medium"
                       FontFamily="Montserrat"
                       Margin="0,10,0,5"/>
            
            <TextBox Name="EdittxtNazwaProduktu" Margin="0,0,0,5"/>

            <TextBlock Text="Cena:"
                       Foreground="White"
                       FontSize="12"
                       FontWeight="Medium"
                       FontFamily="Montserrat"
                       Margin="0,0,0,5"/>
            
            <TextBox Name="EdittxtCena" Margin="0,0,0,5"/>

            <TextBlock Text="Opis:"
                       Foreground="White"
                       FontSize="12"
                       FontWeight="Medium"
                       FontFamily="Montserrat"
                       Margin="0,0,0,5"/>

            <TextBox Name="EdittxtOpis" Margin="0,0,0,5" Height="100" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"/>

            <TextBlock Text="Kategoria:"
                       Foreground="White"
                       FontSize="12"
                       FontWeight="Medium"
                       FontFamily="Montserrat"
                       Margin="0,0,0,5"/>
            
            <ComboBox Name="EditcbKategoria" Margin="0,0,0,5"></ComboBox>

            <Button x:Name="EditbtnWybierzZdjecie" Content="Wybierz zdjęcie" Click="EditbtnWybierzZdjecie_Click" Margin="0,10,0,5" Style="{StaticResource GreenButton}"/>

            <Image Name="EditimgProdukt" Height="100" Width="100" Margin="0,5,0,0"/>

            <Button Content="Edytuj produkt" Click="EdytujProdukt_Click" Style="{StaticResource GreenButton}" Margin="20,5,20,0"/>


        </StackPanel>
            


    </Grid>
</UserControl>
