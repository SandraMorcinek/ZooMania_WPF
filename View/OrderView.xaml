<UserControl x:Class="ZooMania.View.OrderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ZooMania.View"
             xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="120"/>
            </Grid.ColumnDefinitions>
            
            <StackPanel Orientation="Horizontal"
                        VerticalAlignment="Top"
                        Grid.Column="0">
                
                <fa:IconImage Icon="Search" Height="20" Width="20" Foreground="{StaticResource plainTextColor3}"/>
                
                <TextBox Text="Wyszukaj..." x:Name="tbSearch"   
                         FontSize="13"
                         Height="24"
                         Width="300"
                         Background="Transparent"
                         FontWeight="Medium"
                         FontFamily="Montserrat"                            
                         Foreground="{StaticResource plainTextColor3}"
                         CaretBrush="{StaticResource plainTextColor3}"
                         BorderBrush="{StaticResource plainTextColor3}"
                         BorderThickness="0,0,0,1"                             
                         VerticalContentAlignment="Center"
                         Margin="5,5,0,0"></TextBox>
                
            </StackPanel>
            
            <Button Grid.Column="1" x:Name="btnSearch"
                    Click="Button_Click"                                    
                    Content="Szukaj"                            
                    Cursor="Hand"
                    VerticalAlignment="Top"
                    Style="{StaticResource GreenButtonStyle}"/>
            
        </Grid>
        <Border Grid.Row="1"
                Background="{StaticResource panelColor}"
                CornerRadius="10" 
                Padding="10">
            <DataGrid x:Name="dgZamowienia" ItemsSource="{Binding Orders}" AutoGenerateColumns="False"
                      Loaded="Window_Loaded"
                      ColumnWidth="*"
                      Background="{StaticResource panelColor}"
                      BorderThickness="0">
                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Foreground" Value="{StaticResource titleColor3}"/>
                        <Setter Property="Padding" Value="10,0,0,10"/>
                        <Setter Property="FontFamily" Value="Montserrat"/>
                        <Setter Property="FontSize" Value="15"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" />
                    <DataGridTextColumn Header="Imię" Binding="{Binding FirstName}" />
                    <DataGridTextColumn Header="Nazwisko" Binding="{Binding LastName}" />
                    <DataGridTextColumn Header="Email" Binding="{Binding Email}" />
                    <DataGridTextColumn Header="Produkt" Binding="{Binding ProductName}" />
                    <DataGridTextColumn Header="Cena produktu" Binding="{Binding ProductPrice}" />
                    <DataGridTextColumn Header="Data zamówienia" Binding="{Binding OrderDate, StringFormat=\{0:dd-MM-yyyy HH:mm:ss\}}" />
                    <DataGridTextColumn Header="Data dostawy" Binding="{Binding ShoppingDate, StringFormat=\{0:dd-MM-yyyy\}}" />
                    <DataGridTextColumn Header="Status" Binding="{Binding Status}" />
                    <DataGridTextColumn Header="Koszt dostawy" Binding="{Binding TransportCost}" />
                    <DataGridTextColumn Header="Do zapłaty" Binding="{Binding ToPay}" />
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
