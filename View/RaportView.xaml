<UserControl x:Class="ZooMania.View.RaportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ZooMania.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Loaded="Window_Loaded">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" Margin="25">

            <TextBlock Text="Generowanie raportów"
                       Foreground="White"
                       FontSize="16"
                       FontWeight="Medium"
                       FontFamily="Montserrat"
                       Margin="0,0,0,20"/>

            <TextBlock Text="Wybierz"
                       Foreground="White"
                       FontSize="12"
                       FontWeight="Medium"
                       FontFamily="Montserrat"
                       Margin="0,0,0,5"/>

            <ListBox x:Name="TableListBox"/>

            <Button Content="Generuj raport" Style="{StaticResource GreenButton}" Margin="25" Click="GenerujRaport_Click"/>

        </StackPanel>

        <StackPanel Grid.Column="1" Margin="25">

            <ListBox x:Name="TableListBox2" Margin="20,58.5,20,20" Height="30">
                <ListBoxItem>Liczba klientów z zamówieniem w określonym dniu</ListBoxItem>
            </ListBox>
            <DatePicker x:Name="DatePicker" HorizontalAlignment="Center" Margin="0,0,0,5" Width="200"/>
            <Button Content="Generuj raport" Style="{StaticResource GreenButton}" Click="GenerujRaport2_Click" Margin="25"/>

        </StackPanel>
            
    </Grid>
</UserControl>
