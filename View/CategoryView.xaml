<UserControl x:Class="ZooMania.View.CategoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ZooMania.View"
             xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Loaded="dgCategory_Loaded">
    <Grid>

        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Column="0" Grid.Row="0" Margin="25">

            <TextBlock Text="Dodaj kategorię"
                       Foreground="White"
                       FontSize="16"
                       FontWeight="Medium"
                       FontFamily="Montserrat"
                       Margin="0,0,0,15"/>

            <TextBlock Text="Nazwa kategorii"
                       Foreground="White"
                       FontSize="12"
                       FontWeight="Medium"
                       FontFamily="Montserrat"
                       Margin="0,0,0,5"/>

            <TextBox Name="CategoryTextBox"/>

            <Button Content="Dodaj" Margin="15" Click="btnAddCategory_Click" Style="{StaticResource GreenButton}"/>

        </StackPanel>

        <StackPanel Margin="25" Grid.Column="0" Grid.Row="1">

            <TextBlock Text="Edytuj kategorię"
                       Foreground="White"
                       FontSize="16"
                       FontWeight="Medium"
                       FontFamily="Montserrat"
                       Margin="0,0,0,15"/>

            <StackPanel Orientation="Horizontal">

                <TextBlock Text="Id:"
                           Foreground="White"
                           FontSize="12"
                           FontWeight="Medium"
                           FontFamily="Montserrat"
                           Margin="5"/>

                <TextBox x:Name="txtCategoryId" Width="200" Height="20" Margin="10,0,20,0"/>

                <Button Content="Wczytaj" Click="btnWczytaj_Click" Style="{StaticResource GreenButton}" Width="80"/>

            </StackPanel>

            <TextBlock Text="Nazwa"
                       Foreground="White"
                       FontSize="12"
                       FontWeight="Medium"
                       FontFamily="Montserrat"
                       Margin="0,10,0,5"/>

            <TextBox x:Name="txtCategoryName"/>

            <Button Content="Edytuj" Click="btnZapisz_Click" Margin="15" Style="{StaticResource GreenButton}"/>

        </StackPanel>


        <StackPanel Grid.Column="1" Grid.Row="0" Grid.RowSpan="2">

            <StackPanel Margin="10">

                <Button Margin="25" Style="{StaticResource GreenButtonStyle}" Click="RefreshButton_Click">
                    <fa:IconImage Icon="Refresh" Height="20" Foreground="White"/>
                </Button>

            </StackPanel>

            <StackPanel Margin="25">

                <DataGrid x:Name="dgCategory" AutoGenerateColumns="False" ItemsSource="{Binding}" Height="310">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding Id}" />
                        <DataGridTextColumn Header="Nazwa" Binding="{Binding Name}" />
                        <DataGridTextColumn Header="Liczba produktów" Binding="{Binding ProductCount}" />
                    </DataGrid.Columns>
                </DataGrid>

            </StackPanel>

        </StackPanel>

    </Grid>
</UserControl>
